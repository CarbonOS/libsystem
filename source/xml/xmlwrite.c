#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "private.h"
#include "xmlparser.h"
#include "xml.h"

const char *XmlSave(XMLDOCPTR doc, const char *filename)
{
	RETURN_IF_NULL(doc);
	RETURN_IF_NULL(filename);
	return 0;
}

